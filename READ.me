
//playground 


src/
├── app/
│   └── playground/                 # NEW
│       └── page.tsx               # Standalone MVP playground
│
├── modules/
│   └── typingTest/
│       ├── engine.ts              # Core logic ✅ (already built)
│       ├── sentenceBank.ts       # Sentence source ✅ or WIP
│       └── useTypingTest.ts      # Hook to glue logic to React
│
├── components/
│   └── TypingBox.tsx             # Renders the sentence + captures input



// onboarding

src/
├── app/
│   ├── onboarding/                      # Onboarding route entry
│   │   └── page.tsx                     # Renders the onboarding flow
│
├── components/
│   └── QuestionCard.tsx                 # Renders one question and its options
│
├── modules/
│   └── onboarding/
│       ├── questions.ts                 # Static question + option list ✅ (already added)
│       └── useOnboarding.ts             # Hook: manages state, progress, and answers
│
├── app/
│   └── api/
│       └── onboarding/
│           └── submit/route.ts          # [LATER] API route to save responses

// SupaBase Onboarding 

/src/app/api/auth/[...nextauth] 
/src/components/questionCard.tsx
/src/lib/supabase.ts
/src/modules/onboarding/useOnboarding.ts
